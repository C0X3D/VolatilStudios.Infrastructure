apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "rediscache.fullname" . }}
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: {{ .Values.ingress.host | default "redis.local" }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "rediscache.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
