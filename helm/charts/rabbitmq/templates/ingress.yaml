apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "rabbitmq.fullname" . }}
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: {{ .Values.ingress.host | default "rabbitmq.local" }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "rabbitmq.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
