trigger: none

variables:
  - group: tf-shared-vars

stages:
  - template: ../../azure-pipelines.client-template.yml
    parameters:
      ClientName: demo
      Environment: dev      
      PoolName: "Default"
